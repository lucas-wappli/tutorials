<odoo>

    <!-- list view -->
    <record id="estate_property_offer_view_tree" model="ir.ui.view">
        <field name="name">estate.property.offer.list</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <list decoration-success="status == 'accepted'" decoration-danger="status == 'refused'">
                <field name="property_id" width="200" column_invisible="context.get('hide_property')"/>
                <field name="price" width="100"/>
                <field name="partner_id" width="200"/>
                <field name="validity" width="100"/>
                <field name="deadline" width="100"/>
                <field name="status" optional="hide"/>
            </list>
        </field>
    </record>

    <!-- form view -->
    <record id="estate_property_offer_view_form" model="ir.ui.view">
        <field name="name">estate.property.offer.form</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="price"/>
                        <field name="partner_id"/>
                        <field name="validity"/>
                        <field name="deadline"/>
                        <field name="status"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    </odoo>