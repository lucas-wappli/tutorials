<?xml version="1.0"?>
<odoo>

    <!-- list view -->
    <record id="estate_property_view_tree" model="ir.ui.view">
        <field name="name">estate.property.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list decoration-success="state in ['offer_received', 'offer_accepted']" decoration-bf="state == 'offer_accepted'" decoration-muted="state in ['sold']">
                <field name="sequence" widget="handle"/>
                <field name="name" width="300"/>
                <field name="postcode" width="50"/>
                <field name="bedrooms" width="50"/>
                <field name="living_area" width="100"/>
                <field name="expected_price" width="100"/>
                <field name="selling_price" width="100"/>
                <field name="date_availability" width="100" optional="hide"/>
            </list>
        </field>
    </record>

    <!-- form view -->
    <record id="estate_property_view_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_sold" string="Sold" type="object" invisible="state in ['offer_accepted', 'sold', 'canceled']"/>
                    <button name="action_sold" string="Sold" class="btn-primary" type="object" invisible="state != 'offer_accepted'"/>
                    <button name="action_cancel" string="Canceled" type="object" invisible="state in ['sold', 'canceled']"/>
                    <field name="state" widget="statusbar" statusbar_visible="new, offer_received, offer_accepted, sold"/>
                </header>
                <sheet>
                    <h1>
                        <field name="name"/>
                    </h1>
                    <group>
                        <group>
                            <field name='tag_ids' widget='many2many_tags' options="{'color_field': 'color'}"/>
                            <field name="type_id" widget="many2one" options="{'no_create': True}"/>
                            <field name="postcode"/>
                            <field name="date_availability"/>
                        </group>
                        <group>
                            <field name="expected_price"/>
                            <field name="best_offer"/>
                            <field name="selling_price"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Description">
                            <group>
                                <field name="description"/>
                                <field name="bedrooms"/>
                                <field name="living_area"/>
                                <field name="facades"/>
                                <field name="garage"/>
                                <field name="garden"/>
                                <field name="garden_area" invisible="not garden"/>
                                <field name="garden_orientation" invisible="not garden"/>
                                <field name="total_area"/> 
                            </group>
                        </page>
                        <page string="Offers">
                            <field name="offer_ids" readonly="state in ['offer_accepted', 'sold', 'canceled']"/>
                        </page>
                        <page string="Other info">
                            <group>
                                <field name="salesman_id"/>
                                <field name="buyer_id"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- search view -->
    <record id="estate_property_view_search" model="ir.ui.view">
        <field name="name">estate.property.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search>
                <field name="name" />
                <field name="postcode" />
                <field name="expected_price" />
                <field name="bedrooms" />
                <field name="living_area" filter_domain="[('living_area', '>=', self)]"/>
                <field name="facades" />
                <separator />
                <!-- available filter - show properties with available date before today and state not in sold or canceled -->
                <filter string="Available" name="available"
                        domain="[('date_availability', '&lt;=', context_today()), ('state', 'not in', ['sold', 'canceled'])]"/>
                <filter string="Postcode" name="group_by_postcode" context="{'group_by': 'postcode'}"/>
            </search>
        </field>
    </record>

    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="estate.estate_property_view_search"/>
        <field name="context">{'search_default_available': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Click to create a new property.
            </p>
        </field>

    </record>

    <menuitem id="estate_property_menu_root" 
              name="Real Estate"/>

    <menuitem id="estate_menu_first_level" 
              name="Advertissements" 
              parent="estate_property_menu_root"/>

    <menuitem id="estate_menu_first_level_2" 
              name="Settings" 
              parent="estate_property_menu_root"/>

    <menuitem id="estate_menu_properties"
              name="Properties"
              action="estate_property_action"
              parent="estate_menu_first_level"/>

    <menuitem id="estate_menu_property_types"
              name="Property Types"
              action="estate_property_type_action"
              parent="estate_menu_first_level_2"/>

    <menuitem id="estate_menu_property_tags"
              name="Property Tags"
              action="estate_property_tag_action"
              parent="estate_menu_first_level_2"/>
</odoo>
